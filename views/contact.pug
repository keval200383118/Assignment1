extends layout

block content

 div(style='margin:20px')
  .textCenter
    h2(style='margin:20px')  Connect with me 
    h3(style='margin:20px')
      | I would love to hearing back from you. Please Fill the below form to know more. I promise it would not take much time
  form
    .form-group
      label(for='emailAddress') Email address
      input#emailAddress.form-control.form-control.sm(type='email', placeholder='name@example.com')
    .form-group
      label(for='name') Name
      input#name.form-control(type='text', placeholder='John')
    .form-group
      label(for='message') Message
      input#message.form-control(type='text', placeholder='Message')
    button.btn.btn-primary(type='submit') Submit